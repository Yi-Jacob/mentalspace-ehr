export enum ConversationCategory {
  CLINICAL = 'clinical',
  ADMINISTRATIVE = 'administrative',
  URGENT = 'urgent',
  GENERAL = 'general',
  GROUP = 'group',
}

export enum ConversationPriority {
  LOW = 'low',
  NORMAL = 'normal',
  HIGH = 'high',
  URGENT = 'urgent',
}
