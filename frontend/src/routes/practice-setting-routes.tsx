import Settings from '../pages/settings';
import { RouteObject } from 'react-router-dom';

const practiceSettingRoutes: RouteObject[] = [
  {
    index: true,
    element: <Settings />,
  },
];

export default practiceSettingRoutes; 